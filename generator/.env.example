# Free API key from https://aistudio.google.com/app/apikey — no card required.
# Never commit the real value.
GEMINI_API_KEY= . . .

# Optional: override which model generates the cases (defaults to gemini-2.5-flash,
# which is covered by Google AI Studio's free tier).
GENERATOR_MODEL=gemini-2.5-flash
