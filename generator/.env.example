# API key from https://console.anthropic.com/ — never commit the real value.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override which model generates the cases (defaults to claude-sonnet-5).
GENERATOR_MODEL=claude-sonnet-5
