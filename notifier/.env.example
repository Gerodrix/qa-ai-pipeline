# Webhook URL from the n8n workflow that receives run summaries and
# decides what to do with them (e.g. post to Slack when something failed).
N8N_WEBHOOK_URL=https://your-n8n-instance/webhook/qa-pipeline
